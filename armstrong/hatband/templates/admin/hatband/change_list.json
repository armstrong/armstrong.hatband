{
    "results": [
        {% for item in cl.result_list %}
        {
            "value": "{{ item.pk }}",
            "label": "{{ item }}"
        }{% if not forloop.last %},{% endif %}
        {% endfor %}
    ]
}
